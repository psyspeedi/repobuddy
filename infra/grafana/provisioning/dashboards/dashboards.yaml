# Dashboard provider — reads JSON files from /var/lib/grafana/dashboards.
apiVersion: 1

providers:
  - name: codegraph
    orgId: 1
    folder: RepoBuddy
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards
